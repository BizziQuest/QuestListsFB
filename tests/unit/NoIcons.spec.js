import { shallowMount } from '@vue/test-utils';
import ListItem from '@/components/ListItem.vue';

describe('List Item.vue', () => {
  test('no icons on empty input', () => {
    const wrapper = shallowMount(ListItem);
    
  });
});
